{
  "name": "redgrid",
  "private": true,
  "version": "0.0.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "pnpm --filter @redgrid/mobile dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck"
  }
}
